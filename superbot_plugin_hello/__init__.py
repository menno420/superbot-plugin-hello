"""superbot-plugin-hello — the game-plugin-contract hello-world.

Everything lives in ``superbot_plugin_hello.manifest`` (the ``sb.plugins``
entry-point target). This package deliberately contains nothing else: a
plugin is pure declarations + handler registrations, exactly like an
in-tree ``sb/manifest/<key>.py`` module (host design-spec §1.1).
"""
